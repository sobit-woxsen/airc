import { ServicesClient } from "@/app/services/client"

export const metadata = {
  title: "Services — AI Research Center",
  description: "Products, research collaborations, and consulting services for enterprises.",
}

export default function ServicesPage() {
  return <ServicesClient />
}
